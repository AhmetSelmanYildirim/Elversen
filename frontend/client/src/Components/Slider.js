import React from 'react';
import { styles } from '../Styles/styles';

const Slider = () => {
    return (
        <div className='Slider' style={styles.slider} >
            <p>Fotoğraf Gelebilir</p>
        </div>
    )
}

export default Slider
